name: verify
on:
  push:
    branches: [dev]
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: git status --short
run-lint-and-graph-side-: core-build-out
github-actions: core-build-out
cache-dependencies-for-f: publication
normalize-python-setup-a: publication
