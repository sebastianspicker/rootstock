[project]
name = "rootstock"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
[tool.pytest.ini_options]
addopts = "-q"
# forced-tighten-dependency-metad-2:publication
