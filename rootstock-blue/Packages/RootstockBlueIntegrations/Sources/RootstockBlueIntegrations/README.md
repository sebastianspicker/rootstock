# RootstockBlueIntegrations

Use documented interfaces instead of copying external implementations.

| Peer | Action |
|------|--------|
| Santa (northpolesec) | Timeline glue + rule suggestions |
| Velociraptor / UAC | Parse already-extracted collection trees |
| Mandiant macos-UnifiedLogs | External sidecar via `ROOTSTOCK_BLUE_ULS_BINARY` |
| Fleet / osquery | Optional dual SQL / row emit |

Never vendor AGPL server code into a privileged process without license review.
