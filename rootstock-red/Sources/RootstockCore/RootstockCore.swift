/// Rootstock Red core: versioned findings, module contracts, and safety rails.
public enum RootstockCore {
    /// Semantic version of the Rootstock Red platform release.
    public static let version = "0.1.0"
    /// Frozen Finding / CollectedState schema version.
    public static let schemaVersion = "1.0.0"
}
